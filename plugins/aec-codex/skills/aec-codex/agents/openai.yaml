interface:
  display_name: "AEC Codex"
  short_description: "Set up and control local Autodesk sessions"
  default_prompt: "Use $aec-codex to check this Windows PC, set up the local host with my approval if needed, and then inspect Revit or AutoCAD safely."
